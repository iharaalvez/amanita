# Living Pokedex Tracker

## Setup

1. `npm install`
2. `npm run backend`        # starts Docker db + api
3. `npm run seed`           # one-time data seed (~5 mins)
4. `npm run dev`

The frontend runs at <http://localhost:3000>. The local API runs at <http://localhost:8000/api>.
